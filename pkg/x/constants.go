package x

const (
	AppName        string = "aserto-test"
	AppDisplayName string = "Aserto Test"
	AppDescription string = "Aserto Test Runner"
)

// OPA constants.
const (
	// TestPrefix declares the prefix for all test rules.
	TestPrefix string = "test_"
	// SkipTestPrefix declares the prefix for tests that should be skipped.
	SkipTestPrefix string = "todo_test_"
)

// --format enum values.
const (
	FormatText = "text"
	FormatJSON = "json"
)
